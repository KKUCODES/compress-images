interface:
  display_name: "Image Compressor · 图片压缩"
  short_description: "使用 Sharp 在本地压缩单张或批量图片，保留原图并报告节省空间"
  default_prompt: "Use $compress-images to compress the selected local images and report the space saved."
